"""Pydantic request/response schemas (legacy stub).

Most routers now define their own schemas inline. Kept as an empty
package so any lingering `from app.schemas import ...` paths fail
loudly rather than silently importing nothing.
"""
